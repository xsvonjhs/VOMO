using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlphaScript : MonoBehaviour {
    /*
    public KeyCode increaseAlpha;
    public KeyCode decreaseAlpha;
    public float alphaLevel;
    public string abc;

	// Use this for initialization
	void Start () {
        abc = GameObject.Find("FrameChoose").GetComponent<ScreenShot>().Alpha;
        //alpha.Alpha = ;

        if(abc == "true")
        {
            GetComponent<SpriteRenderer>().color = new Color(1, 1, 1, 0);
        }

        else if(abc == "false")
        {
            GetComponent<SpriteRenderer>().color = new Color(1, 1, 1, 255);
        }
	}
	
	// Update is called once per frame
	void Update () {
		
	}
    */
}
